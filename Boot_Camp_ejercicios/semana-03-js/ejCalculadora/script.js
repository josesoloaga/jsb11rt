"use strict";

//Crea una función calculadora que recibirá 3 datos: dos números y un "operador" (string) y hará el calcula correspondiente al operador con los números (ej: calcular(3, "+", 5) devuelve 8 // calcular(8, "-", 4) devuelve 4)

//Como mínimo debe sumar, restar, multiplicar y dividir.

// Al terminar, modificarla para que los datos los introduzca el usuario

//"+", "-"...
//"más", "menos"...
// "sumar", "restar"...
//...

/*
function calculadora(num1, operador, num2) {
  if (operador === "+") {
    return (num1 += num2);
  } else if (operador === "-") {
    return (num1 -= num2);
  } else if (operador === "/") {
    return (num1 /= num2);
  } else if (operador === "*") {
    return (num1 *= num2);
  }
  
}

console.log(calculadora(3, "-", 5));
*/

/*
function calculadora (num1, operador, num2){
  switch (operador){
    case:"+":
      return num1 + num2
    case:"-":
      return num1 - num2
    case:"*":
    case:"/":
  }
}

*/