"use strict";

//SCOPE o alcance de VARIABLES

let variableGlobal = "global";
let variable = "global";
let variableLocal = "global";

console.log(variableGlobal, variable, variableLocal);

if (true) {
}
